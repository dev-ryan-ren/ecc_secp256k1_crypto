platform :ios, '9.0'

project 'ecc_secp256k1_crypto.xcodeproj'

# 去掉由pod引入的第三方库的警告，需要更新命令才生效
inhibit_all_warnings!

target 'ecc_secp256k1_crypto' do

pod 'BCGenerator'
pod 'CBSecp256k1'
pod 'CBBase58'

end
